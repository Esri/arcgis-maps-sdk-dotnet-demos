<Page
    x:Class="OfflineWorkflowSample.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OfflineWorkflowSample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:OfflineWorkflowSample.ViewModels"
    mc:Ignorable="d">
    <Page.Resources>
        <Style x:Key="LoginLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Right" />
        </Style>
        <viewModels:LoginViewModel x:Key="ViewModel" />
    </Page.Resources>

    <Grid Background="{ThemeResource SystemControlAcrylicWindowBrush}" DataContext="{StaticResource ViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <local:CustomAppTitleBar Grid.Row="0" />
        <Grid Grid.Row="1">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                        Orientation="Vertical">
                <TextBlock Text="ArcGIS Maps Offline"
                           Style="{StaticResource HeaderTextBlockStyle}"
                           TextAlignment="Center"
                           Margin="20" />
                <Grid ColumnSpacing="5" RowSpacing="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Button Content="Sign in to ArcGIS Online"
                            Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                            HorizontalAlignment="Stretch"
                            Command="{Binding LogInToPortalCommand}"/>
                    <Button Content="Sign in to Enterprise" 
                            Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                            HorizontalAlignment="Stretch"
                            Command="{Binding ShowEnterpriseFormCommand}"/>
                    <TextBox Text="{Binding PortalUrl, Mode=TwoWay}" Grid.Row="2" Visibility="{Binding PortalFormIsOpen}"/>
                    <Button Content="Continue" Grid.Row="2" Grid.Column="1"
                            Visibility="{Binding PortalFormIsOpen}"
                            HorizontalAlignment="Stretch"
                            Command="{Binding LogInToEnterpriseCommand}"/>
                </Grid>

            </StackPanel>
        </Grid>
    </Grid>
</Page>