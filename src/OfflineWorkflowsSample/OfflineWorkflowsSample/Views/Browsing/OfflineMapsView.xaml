<Page
    x:Class="OfflineWorkflowSample.Views.OfflineMapsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid DataContext="{StaticResource MainViewModel}" Padding="5">
        <GridView ItemsSource="{Binding LocalContentViewModel.Items}"
                  ItemTemplate="{StaticResource PortalItemTemplate}"
                  SelectedItem="{Binding Source={StaticResource MainViewModel},Path=SelectedItem,Mode=TwoWay}" />
    </Grid>
</Page>