<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ArcGISMapViewer.Controls.TableView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArcGISMapViewer.Controls"
    xmlns:data="using:Esri.ArcGISRuntime.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <local:WindowPanel Header="Feature Tables">
        <TabView x:Name="TabView" TabItemsSource="{x:Bind Tables, Mode=OneWay}" Grid.Row="1" IsAddTabButtonVisible="False">
            <TabView.TabItemTemplate>
                <DataTemplate x:DataType="data:FeatureTable">
                    <TabViewItem Header="{x:Bind DisplayName}" CloseRequested="TabViewItem_CloseRequested">
                        <local:FeatureTableView Table="{x:Bind}" FeatureActionInvoked="TableViewFeature_Clicked" />
                    </TabViewItem>
                </DataTemplate>
            </TabView.TabItemTemplate>
        </TabView>
    </local:WindowPanel>

</UserControl>
