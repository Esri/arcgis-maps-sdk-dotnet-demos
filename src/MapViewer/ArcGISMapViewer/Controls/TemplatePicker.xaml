<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ArcGISMapViewer.Controls.TemplatePicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArcGISMapViewer.Controls"
    xmlns:tk="using:Esri.ArcGISRuntime.Toolkit.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ListView x:Name="TemplateList" Background="{StaticResource CardBackgroundFillColorSecondaryBrush}" Padding="0" SelectionMode="None" IsItemClickEnabled="True"
                  ItemClick="TemplateList_ItemClick"
                  >
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:TemplateItem">
                    <StackPanel Orientation="Horizontal">
                        <tk:SymbolDisplay Symbol="{x:Bind Symbol}" Width="40" Height="40" Margin="4" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
