<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ArcGISMapViewer.Views.FeatureEditingNewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArcGISMapViewer.Views"
    xmlns:ctrls="using:ArcGISMapViewer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tk="using:Esri.ArcGISRuntime.Toolkit.UI.Controls"
    xmlns:tkui="using:Esri.ArcGISRuntime.Toolkit.UI"
    xmlns:calcite="using:Esri.Calcite.WinUI"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Editor" Style="{StaticResource BaseTextBlockStyle}" FontSize="18" Margin="12" />
        <StackPanel Grid.Row="1">
            <Expander Header="Settings" HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Stretch">
                <StackPanel>
                    <ToggleSwitch x:Name="EnableSnappingSwitch" Header="Enable Snapping" Style="{StaticResource HorizontalToggleSwitchStyle}" />
                    <ToggleSwitch IsEnabled="{x:Bind EnableSnappingSwitch.IsOn}" Header="Geometry Guides" Style="{StaticResource HorizontalToggleSwitchStyle}" Margin="8,0,0,0" />
                    <ToggleSwitch IsEnabled="{x:Bind EnableSnappingSwitch.IsOn}" Header="Feature to feature" Style="{StaticResource HorizontalToggleSwitchStyle}" Margin="8,0,0,0" />
                </StackPanel>

            </Expander>

            <TextBlock Text="Edit Features" Style="{StaticResource BaseTextBlockStyle}" FontSize="16" Margin="12" />

            <StackPanel Orientation="Horizontal" Margin="12" Background="{StaticResource CardBackgroundFillColorSecondaryBrush}">
                <RadioButton Style="{StaticResource ToggleRadioButton}" ToolTipService.ToolTip="Pan">
                    <IconSourceElement IconSource="{calcite:CalciteIconSource Icon=Pan}" />
                </RadioButton>
                <RadioButton Style="{StaticResource ToggleRadioButton}" ToolTipService.ToolTip="Select by point">
                    <IconSourceElement IconSource="{calcite:CalciteIconSource Icon=SelectionSet}" />
                </RadioButton>
                <RadioButton Style="{StaticResource ToggleRadioButton}" ToolTipService.ToolTip="Select by rectangle">
                    <IconSourceElement IconSource="{calcite:CalciteIconSource Icon=CursorMarquee}" />
                </RadioButton>
                <RadioButton Style="{StaticResource ToggleRadioButton}" ToolTipService.ToolTip="Select by lasso">
                    <IconSourceElement IconSource="{calcite:CalciteIconSource Icon=LassoSelect}" />
                </RadioButton>
            </StackPanel>

            <TextBlock Text="Create Features" Style="{StaticResource BaseTextBlockStyle}" FontSize="16" Margin="12" />

            <ctrls:TemplatePicker Layers="{x:Bind VM.GeoModel.OperationalLayers, Mode=OneWay}" />
        </StackPanel>
    </Grid>
</Page>
