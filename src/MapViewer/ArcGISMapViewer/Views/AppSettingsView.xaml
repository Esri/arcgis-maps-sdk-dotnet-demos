<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ArcGISMapViewer.Views.AppSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArcGISMapViewer.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Border Background="{StaticResource CalciteBrandBrush}">
            <TextBlock Text="Settings" FontSize="16" Foreground="{StaticResource TextOnAccentFillColorPrimaryBrush}" Padding="8,0" VerticalAlignment="Center" />
        </Border>
        <StackPanel Grid.Row="1" Margin="12">
            <ComboBox x:Name="styleSelector" Header="Style:" HorizontalAlignment="Stretch" />
            <ComboBox x:Name="themeSelector" Header="Theme:" HorizontalAlignment="Stretch" Margin="0,16,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
